# Playbooks

Procedural notes: **when X happens → ordered steps**.

- Repo: `playbooks/*.md`
- Personal (wins on same id): `~/.mango/playbooks/`
- Tool: `lookup_playbook`

Example: Playwright login then open gated page — never jump the wall first.

Deeper project explanation: [[Architecture]]. Fuzzy search: use `rag_search`.
